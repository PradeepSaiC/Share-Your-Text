services:
  - type: web
    name: sharemytext
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node backend/index.js
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 20.11.1
